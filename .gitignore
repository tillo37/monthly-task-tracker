# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test output
coverage

# Exported tracker backups — user data, never commit
monthly-task-tracker-*.json

# Supabase credentials — the anon key is public, but .env may also hold keys
# that are not. Nothing in here belongs in the repository.
.env
.env.*
!.env.example

# Local Supabase working files
supabase/.branches
supabase/.temp
